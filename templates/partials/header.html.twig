<nav class="navbar navbar-expand-lg fixed-top nav-front">
   <a class="navbar-brand" href="{{path('home')}}">
    <img src="/uploads/UEFA_Euro_2020_Logo_White.svg.png" width="35" height="40" alt="">
   </a>
   <a class="nav-item nav-link" href="{{path('home')}}">Acceuil</a>
   <a class="nav-item nav-link" href="{{path('teams_index')}}">Equipes</a>
   <a class="nav-item nav-link" href="{{path('stades_index')}}">Stades</a>
   <a class="nav-item nav-link" href="{{path('rankings_index')}}">Classements & Résultats</a>
   <a class="nav-item nav-link" href="{{path('stages_index')}}">Phase finale</a>
   <ul class="navbar-nav ml-auto">
      {% if app.user %}
         <li class="nav-item dropdown">
            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
               <img src="/uploads/{{app.user.picture}}" alt="avatar de {{app.user.username}}" class="avatar-mini">
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
               <a href="{{path('account_index')}}" class="dropdown-item">Mon profil</a>
               <a href="{{path('account_edit')}}" class="dropdown-item">Modifier mon profil</a>
               <div class="dropdown-divider"></div>
               <a href="{{path('app_logout')}}" class="dropdown-item">Déconnexion</a>
            </div>
         </li>
      {% else %}
         <a href="{{path('account_register')}}" class="nav-item nav-link">S'inscrire</a>
         <a href="{{path('app_login')}}" class="nav-item nav-link">Se connecter</a>
      {% endif %}
   </ul>
</nav>